---
  title: Japan
  body_classes: home
  meta_description:
  meta_keywords:
---

.Header
  Itinerary — Japan

- data.dates.sort{ |a, b| a.to_s.downcase <=> b.to_s.downcase }.each do | slug, date |
  = partial 'components/day_summary', locals: { slug: slug, date: date }
